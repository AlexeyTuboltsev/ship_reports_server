# Server
PORT=8080

# Admin UI — change these before exposing to a network
ADMIN_USER=admin
ADMIN_PASSWORD=changeme

# Optional: fetch intervals and retention (uncomment to override defaults)
#OSMC_FETCH_INTERVAL=900    # seconds between OSMC fetches (default: 900)
#NDBC_FETCH_INTERVAL=300    # seconds between NDBC fetches (default: 300)
#MAX_OBS_AGE_HOURS=12       # discard observations older than this (default: 12)

# Optional: data file paths (default: /data/*.jsonl inside the container)
#SETTINGS_FILE=/data/settings.json
#FETCH_HISTORY_FILE=/data/fetch_history.jsonl
#REQUEST_LOG_FILE=/data/request_log.jsonl
